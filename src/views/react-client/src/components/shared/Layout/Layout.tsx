import Root from '@/components/pages/Root';
import Search from './components/Search';

const Layout = () => {
  return (
    <div>
      <Search />
      <Root />
    </div>
  );
};

export default Layout;
