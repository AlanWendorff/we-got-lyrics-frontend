export type TLocalStorageArtistModel = {
  thumbnail: string;
  name: string;
  id: number;
};

export type TLocalStorageSongModel = {
  thumbnail: string;
  name: string;
  owner: string;
  id: number;
};
